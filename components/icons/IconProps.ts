export interface IconProps {
  className?: string;
  /** Width/height in px. Default 24. */
  size?: number;
  /** Stroke width for outline icons. Default varies per icon. */
  strokeWidth?: number;
}
