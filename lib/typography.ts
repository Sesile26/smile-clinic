/**
 * Shared Tailwind class strings for the SmileClinic display type scale.
 * Mirrors the .display-* / .lede tokens from the mockup so headings stay
 * consistent without per-component CSS.
 */
export const displayXl =
  "font-serif font-normal text-[clamp(48px,7.4vw,104px)] leading-[0.98] tracking-[-0.025em]";

export const displayL =
  "font-serif font-normal text-[clamp(40px,5.4vw,72px)] leading-[1.02] tracking-[-0.02em]";

export const displayM =
  "font-serif font-normal text-[clamp(28px,3.4vw,44px)] leading-[1.1] tracking-[-0.015em]";

export const lede = "text-[18px] leading-[1.55] text-navy-400 max-w-[52ch]";
